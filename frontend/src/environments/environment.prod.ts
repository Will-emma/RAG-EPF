// Utilisé par `ng build` (configuration production, voir angular.json).
// URL du backend Render : https://<nom du service>.onrender.com/api
// (nom du service défini dans render.yaml ; à adapter si Render en attribue un autre).
export const environment = {
  production: true,
  apiUrl: 'https://epf-study-ai-api.onrender.com/api',
};
